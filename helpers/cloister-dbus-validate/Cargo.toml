[dependencies]
zbus = {version = "5", default-features = false, features = ["tokio"]}
tokio = {version = "1", features = ["rt-multi-thread", "macros"]}

[package]
name = "cloister-dbus-validate"
version = "0.1.0"
edition = "2021"

[profile.release]
strip = true
lto = true
