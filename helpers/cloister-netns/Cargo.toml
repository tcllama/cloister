[dependencies]
nix = {version = "0.31.3", default-features = false, features = ["sched"]}

[package]
name = "cloister-netns"
version = "0.1.0"
edition = "2021"

[profile.release]
strip = true
lto = true
